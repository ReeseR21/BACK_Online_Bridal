# from django.urls import path
# from . import views
# from .views import Listservice
# url pattern when jwt-token required.

# urlpatterns = [
#     path('', views.VendorList.as_view())
#    ]

# Url pattern used with get all bridal info when no token required.

urlpatterns = [
    # path('all/', views.get_all_onlinebridal),
    # path('', views.vendor),
    # path('listservice/', Listservice.as_view()),

# urlpatterns = [
#     path('all/', views.get_all_vendors),
#     path('', views.user_vendors),
    # path('udate/<int:id>/', views.update)
   ]
    # path('update/<int:vendors_id>/', views.edit, name = 'edit'),
    # path('delete/<int:vendors_id>/', views.delete, name = 'delete')